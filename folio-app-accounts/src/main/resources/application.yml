spring:
  application:
    name: folio-app-accounts
  cache:
    type: caffeine
    cache-names:
      - accounts
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=10m,recordStats
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/folio-app-accounts}
    username: ${SPRING_DATASOURCE_USERNAME:folio-user}
    password: ${SPRING_DATASOURCE_PASSWORD:folio-pass}
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: ${SPRING_RABBITMQ_USERNAME:folio-user}
    password: ${SPRING_RABBITMQ_PASSWORD:folio-pass}

rabbitmq:
  queue:
    name: ${RABBITMQ_QUEUE:accounts.queue}

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  metrics:
    enable:
      cache: true
      caffeine: true
    tags:
      application: ${spring.application.name}