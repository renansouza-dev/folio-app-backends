name: Java CI with Maven

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:
    uses: ./github/workflows/reusable/build-and-cache.yml@develop

  unit-test:
    needs: build
    uses: ./github/workflows/reusable/unit-test.yml@develop

  integration-test:
    needs: build
    uses: ./github/workflows/reusable/integration-test.yml@develop

  code-analysis:
    needs: [unit-test, integration-test]
    uses: ./github/workflows/reusable/code-analysis.yml@develop